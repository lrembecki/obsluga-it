import { OnTopDetector } from './on-top-detector';

describe('OnTopDetector', () => {
  it('should create an instance', () => {
    const directive = new OnTopDetector();
    expect(directive).toBeTruthy();
  });
});
