import { TranslateFileUrlPipe } from './translate-file-url-pipe';

describe('TranslateFileUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFileUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
