{
  "name": "core-session-store",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/core/session-store/src",
  "projectType": "library",
  "tags": ["scope:core", "type:session-store"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/core/session-store/**/*.ts"]
      }
    }
  }
}
